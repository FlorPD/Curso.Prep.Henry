Prototype: Cada clase tiene una propiedad llamada prototype que permite crear un metodo una sola vez y
           permitir que todos los objetos de esa clase tengan acceso al metodo.

Constructors: Es una plantilla para la creacion de objetos siguiendo un modelo predefinido.
              Sirven para crear muchos objetos que comparten algunas propiedades y métodos iguales.
